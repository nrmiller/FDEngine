package net.nicksneurons.tools;

/**
 * TODO Document
 * TODO Consider adding more event callbacks.
 */
public interface GLWindowListener {

    void onWindowOpened(GLWindow source);
    void onWindowClosing(GLWindow source);
    void onWindowClosed(GLWindow source);
}
