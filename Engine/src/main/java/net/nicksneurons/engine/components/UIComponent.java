package net.nicksneurons.engine.components;

import net.nicksneurons.engine.framework.Component;

/**
 * TODO We need to implement this class.
 * It is the root-element of a UI.
 * Subclasses will exist that can be added to the UIComponent in order to build a visual/logical tree.
 * The UIComponent is a special type of component that is rendered separately from things with mesh.
 * This is because the UIComponent requires complex levels of nesting may use layouts.
 */
public class UIComponent extends Component {
}
